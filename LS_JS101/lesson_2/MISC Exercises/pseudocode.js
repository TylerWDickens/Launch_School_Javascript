/* - A function that reutrns the sum of two numbers
- A function that takes an array of strings, andreturns a string that is all those strings concatenated together
- A function that takes an array of integers, and returns a new array with every other element*/

//A function that reutrns the sum of two numbers

/*
START

#given two numbers are passed to a function called number1 and number2

GET number1 and number2

SET sum = the sum of number1 and number2

PRINT sum

END

*/

//A function that takes an array of strings, 
// and returns a string that is all those strings concatenated together

/*
START

GET array = array of strings passed to function

SET concat = to empty string - variable to hold concatenated strings
SET iterator = 0

WHILE iterator <= length of array
  SET concat = concat + value of array at space iterator

  iterator = iterator + 1

PRINT concat

END
*/

// A function that takes an array of integers, 
// and returns a new array with every other element

/*

START

GET array = array of integers passed to function

SET arrEveryOther = empty array to hold every other value of arr
SET iterator = 0

WHILE iterator <= length of array
  SET arrEveryOther = push value of array at space iterator to end of array

  iterator = iterator + 2

Print arrEveryOther

END

*/